---
import { storyblokEditable, type SbBlokData } from "@storyblok/astro";
import type { BenefitStoryblok } from "@storyblok/types";
import type { Prettify } from "@/types";
import { parseAsset } from "@/services/storyblok/helpers";

type Props = {
  blok: Prettify<BenefitStoryblok>;
};

const {
  blok: { title, description, icon },
} = Astro.props;
---

<div {...storyblokEditable(Astro.props.blok as SbBlokData)}>
  {
    icon && (
      <div>
        <img src={parseAsset(icon, "src")} alt={parseAsset(icon, "alt")} />
      </div>
    )
  }
  <h2>{title}</h2>
  <p>{description}</p>
</div>
